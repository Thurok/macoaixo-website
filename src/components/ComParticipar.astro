<section class="section" id="per-que">
	<div class="container">
		<h2>Porta Catalunya allà on vagis.</h2>
		<p class="section-intro">
			Com participar:
		</p>

		<div class="features">
			<article class="feature-card">
				<div class="feature-icon">1</div>
				<h3>Sol·licita adhesius</h3>
				<p>Recopila els teus adhesius per al teu proper viatge. Compra'ls <a href="https://ko-fi.com/macoaixo" target="_blank" rel="noreferrer">aquí</a> o envian's un missatge!<br>
				No oblidis posar-los a la maleta!</p>
			</article>

			<article class="feature-card">
				<div class="feature-icon">2</div>
				<h3>Col·loca adhesius a l'estranger</h3>
				<p>
					Troba un lloc pintoresc, posa-hi el teu adhesiu (allà on estigui permès sense fer res malbé).<br>Sigues creatiu i respectuós amb l'entorn, la meitat de la diversió és trobar el lloc ideal!
				</p>
			</article>

			<article class="feature-card">
				<div class="feature-icon">3</div>
				<h3>Grava un vídeo</h3>
				<p>Comença amb un primer pla de l'adhesiu i després enfoc a les vistes. Envia'ns el teu vídeo per xarxes socials o per correu electrònic i el compartirem al nostre perfil.<br>
				Fes també un post i etiqueta'ns!</p>
			</article>
		</div>
	</div>
</section>

<style>
	.section {
		padding: clamp(2.4rem, 6vw, 4.6rem) 0;
	}

	h2 {
		font-size: clamp(1.7rem, 4.5vw, 2.7rem);
		line-height: 1.1;
		letter-spacing: -0.03em;
		margin: 0;
	}

	.section-intro {
		max-width: 68ch;
		margin: 0.8rem 0 1.4rem;
		color: var(--muted);
	}

	.features {
		display: grid;
		grid-template-columns: repeat(3, minmax(0, 1fr));
		gap: 1rem;
	}

	.feature-card {
		padding: 1.2rem;
		border-radius: 18px;
		background: var(--card);
		border: 1px solid color-mix(in oklab, var(--ink) 10%, white 90%);
		box-shadow: var(--shadow-soft);
	}

	.feature-icon {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		width: 40px;
		height: 40px;
		border-radius: 999px;
		font-weight: 700;
		font-size: 0.86rem;
		background: var(--brand-yellow);
	}

	.feature-card h3 {
		margin: 0.8rem 0 0.45rem;
	}

	.feature-card p {
		margin: 0;
		color: var(--muted);
	}

	@media (max-width: 980px) {
		.features {
			grid-template-columns: 1fr 1fr;
		}
	}

	@media (max-width: 720px) {
		.features {
			grid-template-columns: 1fr;
		}
	}
</style>