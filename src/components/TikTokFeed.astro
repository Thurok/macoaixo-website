<section class="section tiktok-feed" id="xarxa-tiktok" aria-labelledby="tiktok-feed-title">
	<div class="container">
		<div class="feed-header">
			<h2 id="tiktok-feed-title">Ultims TikToks</h2>
		</div>

		<div class="embed-shell">
			<blockquote
				class="tiktok-embed"
				cite="https://www.tiktok.com/@macoaixo.cat"
				data-unique-id="macoaixo.cat"
				data-embed-from="embed_page"
				data-embed-type="creator"
				style="max-width:780px; min-width:288px;"
			>
				<section>
					<a target="_blank" rel="noreferrer" href="https://www.tiktok.com/@macoaixo.cat?refer=creator_embed">
						@macoaixo.cat
					</a>
				</section>
			</blockquote>
			<script async src="https://www.tiktok.com/embed.js"></script>
		</div>

		<p class="fallback-link">
			Si no es carrega l'embed, obre el perfil a
			<a href="https://www.tiktok.com/@macoaixo.cat" target="_blank" rel="noreferrer">TikTok</a>.
		</p>
	</div>
</section>

<style>
	.section {
		padding: clamp(2.2rem, 6vw, 4.2rem) 0;
	}

	.feed-header {
		display: grid;
		gap: 0.4rem;
		margin-bottom: 1rem;
	}

	h2 {
		font-size: clamp(1.6rem, 4vw, 2.3rem);
		letter-spacing: -0.02em;
		margin: 0;
	}

	p {
		margin: 0;
		color: var(--muted);
	}

	.embed-shell {
		width: 100%;
		display: flex;
		justify-content: center;
		border-radius: 24px;
		overflow: hidden;
		border: 1px solid color-mix(in oklab, var(--ink) 12%, white 88%);
		background: #fff;
		box-shadow: var(--shadow-soft);
	}

	.embed-shell :global(.tiktok-embed) {
		margin: 0 auto !important;
	}

	.fallback-link {
		margin-top: 0.8rem;
		font-size: 0.95rem;
	}

	.fallback-link a {
		color: inherit;
		font-weight: 700;
	}

	@media (min-width: 820px) {
		.embed-shell {
			padding-block: 1.5rem;
		}
	}

</style>